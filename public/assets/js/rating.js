const starRating = [document.getElementsByClassName("star")];

function starSelection(stars) {
    const starClassActive = "value-5";
    const starClassInactive = "value-0";
    const starsLength = stars.length;
    let i;
};

// count total number of clicks
// totalClicks = document.getElementByClassName("rating")
// clicks = 0
//totalClicks = addEventListener("click", function() {
    // clicks +=1
// })


// averageRating =